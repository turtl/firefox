exports.config = {
	// the full URL at which the API can be reached.
	api_url: 'http://tagit.beeets.com/api',

	// the URL of the addon website
	site_url: 'http://tagit.beeets.com',

	// settings for bookmarker
	bookmark: {
	},

	// settings for sync process
	sync: {
	}
};
